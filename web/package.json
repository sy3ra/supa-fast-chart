{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd ../chart_engine && wasm-pack build --target web --out-dir ../web/pkg && cd ../web && vite",
    "build": "cd ../chart_engine && wasm-pack build --target web --out-dir ../web/pkg && cd ../web && vite build"
  },
  "devDependencies": {
    "vite": "^7.2.4",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.5.0"
  }
}